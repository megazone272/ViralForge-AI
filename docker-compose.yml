services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: viralforge
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports: ["5432:5432"]
    volumes: [viralforge_pg:/var/lib/postgresql/data]
volumes:
  viralforge_pg: